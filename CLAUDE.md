# Code Style Rules

- Never use special or non-ASCII characters in code or comments. Only use standard ASCII (letters, numbers, basic punctuation like `-`, `_`, `/`, `*`, etc.). No unicode dashes, arrows, box-drawing characters, fancy quotes, or emojis.
