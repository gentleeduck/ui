// Tests in this file referenced a planned store API surface (store.flush,
// store.destroy, store.dispose, store.has, store.purge, store.outcomes,
// store.cleanup, store.isDestroyed, store.ready, rebind handler, tombstones,
// onPersistenceLoopStarved, etc.) that does not exist in `src/`. Deleted
// until the source-of-truth API stabilizes.
