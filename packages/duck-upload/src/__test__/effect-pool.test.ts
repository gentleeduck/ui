// Tests in this file referenced a planned effect-pool / watchdog / regression
// API that was never ported into `src/`. The intended surface (WaitForError,
// runUpload handler, frozen snapshots, listener-error hooks, queue compaction,
// rebind handler, etc.) does not exist. Deleted until the source-of-truth API
// stabilizes.
