export namespace Navigation {
  /** Direction to navigate: backward or forward. */
  export type Direction = 'prev' | 'next'

  /** Unit to navigate by: one month, one year, or ten years. */
  export type Unit = 'month' | 'year' | 'decade'
}
