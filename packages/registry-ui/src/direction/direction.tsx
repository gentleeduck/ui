'use client'

import {
  DIRECTION_DICTIONARY,
  DirectionContext,
  DirectionProvider,
  Provider,
  useDirection,
} from '@gentleduck/primitives/direction'

export type { IDirection } from '@gentleduck/primitives/direction'
export { DIRECTION_DICTIONARY, DirectionContext, DirectionProvider, Provider, useDirection }
