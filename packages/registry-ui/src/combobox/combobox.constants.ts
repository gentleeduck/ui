/**
 * Maximum number of individually rendered selection badges in the combobox
 * trigger before the trigger collapses to a single `+N Selected` badge.
 * Tune this if the trigger grows visually too wide on multi-select pages.
 */
export const COMBOBOX_MAX_SELECTION_BADGES = 2
