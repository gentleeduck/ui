---
"@gentleduck/cli": minor
---

Add interactive merge GUI with syntax highlighting, split monolithic screen components into workflow and keyboard hooks, extract shared utilities, add JSDoc throughout, fix tsconfig module resolution, sync registry schema with canonical source, and add comprehensive test suite (170 tests).
