---
"@gentleduck/cli": minor
---

Add `--template` flag to the `init` command for scaffolding projects from templates. Run `npx @gentleduck/cli init --template acme` to download and extract the acme monorepo template.
