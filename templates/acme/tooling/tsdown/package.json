{
  "$schema": "https://json.schemastore.org/package.json",
  "engines": {
    "node": ">=22.0.0"
  },
  "author": "acme contributors",
  "dependencies": {
    "@vitejs/plugin-react-swc": "^3.11.0",
    "tsdown": "0.18.3"
  },
  "description": "Shared tsdown build configuration for acme/ui.",
  "devDependencies": {},
  "keywords": [
    "tsdown",
    "tsdown-config"
  ],
  "license": "MIT",
  "main": "dist/index.mjs",
  "name": "@acme/tsdown-config",
  "homepage": "https://gentleduck.org/duck-ui",
  "bugs": {
    "url": "https://github.com/acme/acme-ui/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/acme/acme-ui.git",
    "directory": "tooling/tsdown"
  },
  "packageManager": "bun@1.3.10",
  "private": true,
  "scripts": {
    "build": "tsdown index.ts --format esm --dts",
    "clean": "git clean -xdf .cache .turbo node_modules dist",
    "format": "biome format --write ./",
    "lint": "biome lint --write ./"
  },
  "type": "module",
  "types": "dist/index.d.mts",
  "version": "0.0.1"
}
