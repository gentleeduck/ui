---
title: radio-group
description: A set of checkable buttons—known as radio buttons—where no more than one of the buttons can be checked at a time.
component: true
---

<ComponentPreview
  name="radio-group-1"
  description="A radio group with 3 items."
/>

## Philosophy

Radio groups enforce single selection from a set -- the constraint is the feature. Unlike checkboxes (multi-select) or selects (dropdown), radio groups make all options visible simultaneously. We use native radio inputs for built-in keyboard navigation (arrow keys cycle through options) and form integration.

## How It's Built

<MermaidDiagram chart={`graph TD
  RadioGroup[RadioGroup]
  RadioGroup --> primitives["@gentleduck/primitives\n/checkers"]:::internal
  RadioGroup --> motion["@gentleduck/motion"]:::internal
  RadioGroup --> libs["@gentleduck/libs"]:::internal

  primitives --> |"provides"| SvgIndicator["useSvgIndicator\n(dot indicator)"]
  primitives --> |"provides"| NativeRadio["Native Radio\nInputs"]
  motion --> |"provides"| AnimVariants["Selection\nAnimation"]
  RadioGroup --> |"adds"| ArrowNav["Arrow Key\nNavigation"]
  RadioGroup --> |"adds"| GroupContext["RadioGroup\nContext"]

  classDef external fill:#818cf8,stroke:#6366f1,color:#fff
  classDef internal fill:#2dd4bf,stroke:#14b8a6,color:#fff
`} />

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx @gentleduck/cli add radio-group
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install @gentleduck/primitives @gentleduck/motion @gentleduck/libs
```

<Step>Add the `Label` component to your project.</Step>

The `RadioGroup` component uses the [`Label`](/components/label) component. Make sure you have it installed in your project.

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="radio-group" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</Tabs>

## Usage

```tsx
import { Label } from "@/components/ui/label"
import { RadioGroup, RadioGroupItem } from "@/components/ui/radio-group"
```

```tsx
<RadioGroup defaultValue="option-one">
  <div className="flex items-center space-x-2">
    <RadioGroupItem value="option-one" id="option-one" />
    <Label htmlFor="option-one">Option One</Label>
  </div>
  <div className="flex items-center space-x-2">
    <RadioGroupItem value="option-two" id="option-two" />
    <Label htmlFor="option-two">Option Two</Label>
  </div>
</RadioGroup>
```

## Examples

### Form

<ComponentPreview name="radio-group-2" />

## Component Composition

<MermaidDiagram chart={`graph TD
    A[RadioGroup] --> B[RadioGroupItem]
    B --> C[Radio]
    B --> D[Label]
    C --> E[Hidden Input]
    C --> F[SVG Indicator]
`} />

## API Reference

### RadioGroup

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `value` | `string` | `--` | Controlled value of the selected radio item |
| `defaultValue` | `string` | `--` | Initial selected value when uncontrolled |
| `onValueChange` | `(value: string) => void` | `--` | Callback fired when selection changes |

*Also accepts all standard HTML `ul` props.*

**Behavior:**

* Provides context for radio items.
* Manages selection state (controlled or uncontrolled).
* Renders a `<ul>` with role `radiogroup`.
* Uses internal hooks for managing click behavior and ARIA attributes.

### RadioGroupItem

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `value` | `string` | (required) | Unique value for the radio item, also used as the element's `id` |
| `customIndicator` | `React.ReactNode` | `--` | Custom indicator element rendered inside the item |

*Also accepts all standard HTML `li` props (except `value`).*

**Behavior:**

* Renders a list item representing a single radio option.
* Supports custom indicator and label.
* Handles `aria-checked` state toggling and styling.
* Associates label with radio input via `htmlFor` and `id`.

### Radio

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `indicator` | `React.ReactElement` | `--` | Custom SVG or element for unchecked state indicator |
| `checkedIndicator` | `React.ReactElement` | `--` | Custom SVG or element for checked state indicator |

*Also accepts all standard HTML `input` props.*

**Behavior:**

* Hidden native radio input that manages selection.
* Renders accessible, animated SVG indicators via `useSvgIndicator` hook.
* Supports custom checked and unchecked indicators.
* Applies animation and styling variants.

### useRadioGroupContext

* Hook to consume the `RadioGroupContext`.
* Throws an error if used outside of a `RadioGroup` provider.

### useHandleRadioClick

* Hook to manage click handlers and ARIA states on radio items.
* Accepts optional `defaultValue`, `value`, and `onValueChange` callback.
* Returns refs to the wrapper (`<ul>`), selected item, and all items.

## See also

- [Checkbox](/docs/components/checkbox) — Multi-selection toggle control
- [Switch](/docs/components/switch) — Toggle between on and off states
- [Select](/docs/components/select) — Dropdown selection from a list

