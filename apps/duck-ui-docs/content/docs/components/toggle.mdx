---
title: toggle
description: A two-state button that can be either on or off.
component: true
---

<ComponentPreview
  name="toggle-1"
  description="A toggle component."
/>

## Philosophy

Toggles represent a stateful action -- not navigation, not form submission, but "turn this on or off." The distinction from buttons matters: toggles carry `aria-pressed` state and communicate their current value visually. We integrate with `@gentleduck/variants` to keep visual states clean and type-safe.

## How It's Built

<MermaidDiagram chart={`graph TD
  Toggle[Toggle]
  Toggle --> variants["@gentleduck/variants"]:::internal
  Toggle --> libs["@gentleduck/libs"]:::internal
  variants --> |"provides"| VariantSystem["Variant System\n(default, outline)"]
  variants --> |"provides"| Sizes["Size Variants\n(sm, default, lg)"]
  Toggle --> |"wraps"| NativeInput["Native Checkbox\n(hidden input)"]
  Toggle --> |"manages"| AriaPressed["aria-pressed\nState"]
  classDef internal fill:#2dd4bf,stroke:#14b8a6,color:#fff
`} />

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx @gentleduck/cli add toggle
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install @gentleduck/variants @gentleduck/libs
```

<Step>Add the `Toggle` component to your project.</Step>

The `ToggleGroup` component uses the [`Toggle`](/components/toggle) component. Make sure you have it installed in your project.

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="toggle" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</Tabs>

## Usage

```tsx
import { Toggle } from '@/components/ui/toggle'
```

```tsx
<Toggle>Toggle</Toggle>
```

## Examples

### Default

<ComponentPreview
  name="toggle-2"
  description="A toggle component."
/>

### Outline

<ComponentPreview
  name="toggle-3"
  description="A toggle component using the outline variant."
/>

### With Text

<ComponentPreview
  name="toggle-4"
  description="A toggle component with text."
/>

### Small

<ComponentPreview
  name="toggle-5"
  description="A small toggle component."
/>

### Large

<ComponentPreview
  name="toggle-6"
  description="A large toggle component."
/>

### Disabled

<ComponentPreview
  name="toggle-7"
  description="A disabled toggle component."
/>

## API Reference

### Toggle

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `className` | `string` | -- | Additional CSS class names to apply |
| `value` | `string` | (required) | The value associated with the toggle input |
| `variant` | `'default' \| 'outline'` | `'default'` | Visual style variant of the toggle |
| `size` | `'default' \| 'sm' \| 'lg'` | `'default'` | Size variant of the toggle |
| `disabled` | `boolean` | `false` | Disables the toggle if set to `true` |
| `children` | `React.ReactNode` | -- | Content rendered inside the toggle label |
| `ref` | `React.Ref<HTMLInputElement>` | -- | Ref forwarded to the underlying hidden input element |

*Also accepts all standard HTML `input` props (via `Omit<React.HTMLProps<HTMLInputElement>, 'size'>`).*
