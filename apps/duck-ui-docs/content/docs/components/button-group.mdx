---
title: button-group 
description: A container that groups related button like elements together with consistent styling.
component: true
---

<ComponentPreview
  name="button-group-1"
  description="An email toolbar with nested button groups, featuring archive, report, and snooze actions alongside a dropdown menu for additional options."
/>

## Philosophy

Individual buttons sometimes belong together. ButtonGroup handles the visual joining — shared borders, connected radii, consistent spacing — so you don't manually manage CSS for grouped actions. It's a layout primitive, not a logic primitive: each button inside still owns its own behavior.

## How It's Built

<MermaidDiagram chart={`graph TD
  ButtonGroup[ButtonGroup]
  ButtonGroup --> variants["@gentleduck/variants"]:::internal
  ButtonGroup --> primitives["@gentleduck/primitives\n/slot"]:::internal
  ButtonGroup --> libs["@gentleduck/libs"]:::internal
  ButtonGroup --> Button["Button"]:::component
  ButtonGroup --> Separator["Separator"]:::component
  variants --> |"provides"| LayoutVariants["Layout Variants\n(horizontal, vertical)"]
  primitives --> |"provides"| SlotRendering["Slot Rendering"]
  ButtonGroup --> |"manages"| ConnectedBorders["Connected Borders\n& Radii"]
  classDef internal fill:#2dd4bf,stroke:#14b8a6,color:#fff
  classDef component fill:#fb923c,stroke:#f97316,color:#fff
`} />

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx @gentleduck/cli add button-group
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install @gentleduck/libs @gentleduck/variants
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource path="packages/registry-ui/src/button-group" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</Tabs>

## Usage

```tsx
import {
  ButtonGroup,
  ButtonGroupSeparator,
  ButtonGroupText,
} from "@/components/ui/button-group"
```

```tsx
<ButtonGroup>
  <Button>Button 1</Button>
  <Button>Button 2</Button>
</ButtonGroup>
```

## Accessibility

- The `ButtonGroup` component has the `role` attribute set to `group`.
- Use <Kbd>Tab</Kbd> to navigate between the buttons in the group.
- Use `aria-label` or `aria-labelledby` to label the button group.

```tsx showLineNumbers
<ButtonGroup aria-label="Button group">
  <Button>Button 1</Button>
  <Button>Button 2</Button>
</ButtonGroup>
```

## ButtonGroup vs ToggleGroup

- Use the `ButtonGroup` component when you want to group buttons that perform an action.
- Use the `ToggleGroup` component when you want to group buttons that toggle a state.

## Examples

### Orientation

Set the `orientation` prop to change the button group layout.

<ComponentPreview
  name="button-group-2"
  description="A vertical button group with plus and minus icon buttons stacked on top of each other."
/>

### Size

Control the size of buttons using the `size` prop on individual buttons.

<ComponentPreview
  name="button-group-3"
  description="Button groups rendered at small, default, and large sizes to demonstrate the size prop on individual buttons."
/>

### Nested

Nest `<ButtonGroup>` components to create button groups with spacing.

<ComponentPreview
  name="button-group-4"
  description="Nested button groups combining a pagination group with previous and next arrow buttons."
/>

### Separator

The `ButtonGroupSeparator` component visually divides buttons within a group.

Buttons with variant `outline` do not need a separator since they have a border. For other variants, a separator is recommended to improve the visual hierarchy.

<ComponentPreview
  name="button-group-5"
  description="A button group with a ButtonGroupSeparator dividing Copy and Paste actions using the secondary variant."
/>

### Split

Create a split button group by adding two buttons separated by a `ButtonGroupSeparator`.

<ComponentPreview
  name="button-group-6"
  description="A split button with a primary action and a separator followed by a plus icon button for secondary actions."
/>

### Input

Wrap an `Input` component with buttons.

<ComponentPreview
  name="button-group-7"
  description="A search input paired with a search icon button inside a button group."
/>

### Input Group

Wrap an `InputGroup` component to create complex input layouts.

<ComponentPreview
  name="button-group-8"
  description="A messaging input group with a plus button, text input, and a toggleable voice mode addon inside nested button groups."
/>

### Dropdown Menu

Create a split button group with a `DropdownMenu` component.

<ComponentPreview
  name="button-group-9"
  description="A split button group with a Follow action and a dropdown menu for conversation options like mute, report, and delete."
/>

### Select

Pair with a `Select` component.

<ComponentPreview
  name="button-group-10"
  description="Three currency select dropdowns grouped together with an arrow submit button for currency conversion."
/>

### Popover

Use with a `Popover` component.

<ComponentPreview
  name="button-group-11"
  description="A Copilot button paired with a popover that opens a task description form for starting agent tasks."
/>

## API Reference

### ButtonGroup

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `orientation` | `'horizontal' \| 'vertical'` | `'horizontal'` | Layout direction of the button group |
| `className` | `string` | `--` | Additional class names to apply |
| `children` | `React.ReactNode` | `--` | Grouped content (buttons, separators, text, or nested groups) |
| `...props` | `React.HTMLProps<HTMLDivElement>` | -- | Additional props to spread to the content div |

### ButtonGroupSeparator

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `orientation` | `'horizontal' \| 'vertical'` | `'vertical'` | Orientation of the separator line |
| `className` | `string` | `--` | Additional class names to apply |
| `...props` | `React.ComponentPropsWithoutRef<typeof Separator>` | -- | Additional props inherited from `Separator`. |

### ButtonGroupText

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `asChild` | `boolean` | `false` | If `true`, uses the `Slot` component instead of rendering a `div` |
| `className` | `string` | `--` | Additional class names to apply |
| `children` | `React.ReactNode` | `--` | Text content to display |
| `...props` | `React.HTMLProps<HTMLDivElement>` | -- | Additional props to spread to the content div |

## RTL Support

Direction is resolved through the shared primitives direction module. Use a local `dir="rtl"` override when the component exposes it, or set `DirectionProvider` at app/root level for global RTL/LTR behavior.

<ComponentPreview
  name="button-group-12"
  description="A button group with RTL support."
/>
