---
title: hover-card
description: For sighted users to preview content available behind a link.
component: true
---

<ComponentPreview name="hover-card-1" description="A hover card component" />

## Philosophy

Hover cards provide progressive disclosure without commitment. They let users preview content before clicking, reducing unnecessary navigation. We build on Popover primitives because hover cards are fundamentally popovers with mouse-enter triggers -- shared internals mean consistent behavior and smaller bundle size.

## How It's Built

<MermaidDiagram chart={`graph TD
  HoverCard[HoverCard]
  HoverCard --> primitives["@gentleduck/primitives\n/hover-card"]:::internal
  HoverCard --> motion["@gentleduck/motion"]:::internal
  HoverCard --> libs["@gentleduck/libs"]:::internal
  HoverCard --> Button["Button"]:::component
  primitives --> |"provides"| HoverTrigger["Hover Detection\n& Timing"]
  primitives --> |"provides"| Positioning["Floating UI\nPositioning"]
  motion --> |"provides"| AnimVariants["Entry/Exit\nAnimations"]
  classDef external fill:#818cf8,stroke:#6366f1,color:#fff
  classDef internal fill:#2dd4bf,stroke:#14b8a6,color:#fff
  classDef component fill:#fb923c,stroke:#f97316,color:#fff
`} />

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx @gentleduck/cli add hover-card
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install @gentleduck/primitives @gentleduck/libs @gentleduck/motion @gentleduck/variants  
```

<Step>Add the `Button` component to your project.</Step>

The `HoverCard` component uses the [`Button`](/components/button) component. Make sure you have it installed in your project.

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="hover-card" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</Tabs>

## Usage

```tsx
import {
  HoverCard,
  HoverCardContent,
  HoverCardTrigger,
} from "@/components/ui/hover-card"
```

```tsx
<HoverCard>
  <HoverCardTrigger>Hover</HoverCardTrigger>
  <HoverCardContent>
    The React Framework – created and maintained by @vercel.
  </HoverCardContent>
</HoverCard>
```

## API Reference

### HoverCard

Root wrapper that provides hover-card state and context. Wraps `HoverCardPrimitive.Root` internally.

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `placement` | `Placement` | `'top'` | Position of the hover card relative to the trigger |
| `skipDelayDuration` | `number` | `--` | Time in milliseconds to skip the delay when rapidly moving between triggers |
| `delayDuration` | `number` | `--` | Delay time in milliseconds before showing the card |
| `open` | `boolean` | `--` | Controlled open state |
| `onOpenChange` | `(open: boolean) => void` | `--` | Callback when open state changes |
| `defaultOpen` | `boolean` | `--` | Initial open state for uncontrolled usage |
| `children` | `React.ReactNode` | `--` | HoverCard sub-components |
| `...props` | `React.ComponentPropsWithoutRef<typeof HoverCardPrimitive.Root>` | -- | Additional props inherited from `HoverCard.Root`. |

### HoverCardTrigger

The element that triggers the hover card on hover. Renders a `Button` internally.

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `variant` | `string` | `'outline'` | Variant style applied to the internal `Button` |
| `asChild` | `boolean` | `false` | Whether to use the child as the trigger element directly |
| `children` | `React.ReactNode` | `--` | Content rendered inside the trigger button |
| `...props` | `React.ComponentPropsWithoutRef<typeof HoverCardPrimitive.Trigger> & React.ComponentPropsWithoutRef<typeof Button>` | -- | Additional props inherited from `HoverCard.Trigger` and `Button`. |

### HoverCardContent

The content displayed when the hover card is open. Wraps `HoverCardPrimitive.Content` with animation and styling.

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `className` | `string` | `--` | Additional CSS classes to apply to the content container |
| `children` | `React.ReactNode` | `--` | Content rendered inside the hover card |
| `forceMount` | `boolean` | `true` | Keep content mounted for CSS-driven animations |
| `renderOnce` | `boolean` | `true` | Keep content mounted after first open |
| `lockScroll` | `boolean` | `false` | Locks page scroll while the hover card is open |
| `disabled` | `boolean` | `true` | Disables focus management when true |
| `...props` | `React.ComponentPropsWithoutRef<typeof HoverCardPrimitive.Content> & React.HTMLProps<HTMLDivElement>` | -- | Additional props inherited from `HoverCard.Content` and standard content div props. |

