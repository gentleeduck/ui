---
title: context-menu
description: Displays a context menu of actions triggered by right-click on a target.
component: true
---

<ComponentPreview
  name="context-menu-1"
  description="A context menu with sub menu items."
/>

## Philosophy

Context menus are power-user shortcuts -- they surface relevant actions where the user already is. We share the DropdownMenu component internals because the interaction pattern is identical; only the trigger mechanism (right-click vs button click) differs. This means you learn one API for both patterns.

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx @gentleduck/cli add context-menu
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install @gentleduck/libs 
```
<Step>Make sure you have the [`DropdownMenu`](/docs/components/dropdown-menu) component installed in your project.</Step>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="context-menu" title="components/ui/context-menu.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</Tabs>

## Usage

```tsx showLineNumbers
import {
  ContextMenu,
  ContextMenuContent,
  ContextMenuItem,
  ContextMenuTrigger,
} from "@/components/ui/context-menu"
```

```tsx showLineNumbers
<ContextMenu>
  <ContextMenuTrigger>Right click</ContextMenuTrigger>
  <ContextMenuContent>
    <ContextMenuItem>Profile</ContextMenuItem>
    <ContextMenuItem>Billing</ContextMenuItem>
    <ContextMenuItem>Team</ContextMenuItem>
    <ContextMenuItem>Subscription</ContextMenuItem>
  </ContextMenuContent>
</ContextMenu>
```

## Component Composition

<MermaidDiagram chart={`graph TD
    A[ContextMenu] --> B[ContextMenuTrigger]
    A --> C[ContextMenuContent]
    C --> D[ContextMenuGroup]
    D --> E[ContextMenuItem]
    D --> F[ContextMenuCheckboxItem]
    C --> G[ContextMenuRadioGroup]
    G --> H[ContextMenuRadioItem]
    C --> I[ContextMenuLabel]
    C --> J[ContextMenuSeparator]
    C --> K[ContextMenuSub]
    K --> L[ContextMenuSubTrigger]
    K --> M[ContextMenuSubContent]
`} />

## Behavior

- Right-click the trigger area to open the menu.
- Left-click does not open the menu; use DropdownMenu if you need click activation.
- Right-clicking outside closes the menu and allows the native browser menu to appear.

## API Reference

ContextMenu shares the same props as [DropdownMenu](/docs/components/dropdown-menu). Every ContextMenu sub-component is a thin wrapper around its DropdownMenu counterpart with `contextMenu` enabled by default. See the DropdownMenu API Reference for full prop details.

### ContextMenu

Root wrapper. Renders a `DropdownMenu` with `contextMenu` set to `true`.

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `children` | `React.ReactNode` | -- | Context menu content (trigger + menu content) |
| `...props` | `React.ComponentPropsWithoutRef<typeof DropdownMenu>` | -- | Additional props inherited from `DropdownMenu`. |

### ContextMenuTrigger

The right-click target area. Renders a `DropdownMenuTrigger` with default dimensions and dashed border styling.

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `className` | `string` | -- | Additional CSS classes (merged with default `h-[200px] w-[300px]` styling) |
| `children` | `React.ReactNode` | -- | Trigger area content |
| `...props` | `React.ComponentPropsWithoutRef<typeof DropdownMenuTrigger>` | -- | Additional props inherited from `DropdownMenuTrigger`. |

### ContextMenuContent

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `className` | `string` | -- | Additional CSS classes (merged with `fixed` positioning) |
| `children` | `React.ReactNode` | -- | Menu items and groups |
| `...props` | `React.ComponentPropsWithoutRef<typeof DropdownMenuContent>` | -- | Additional props inherited from `DropdownMenuContent`. |

### ContextMenuItem

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `className` | `string` | -- | Additional CSS classes |
| `children` | `React.ReactNode` | -- | Item content |
| `...props` | `React.ComponentPropsWithoutRef<typeof DropdownMenuItem>` | -- | Additional props inherited from `DropdownMenuItem`. |

### ContextMenuCheckboxItem

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `checked` | `boolean` | -- | Whether the item is checked |
| `onCheckedChange` | `(checked: boolean) => void` | -- | Callback when checked state changes |
| `children` | `React.ReactNode` | -- | Item content |
| `...props` | `React.ComponentPropsWithoutRef<typeof DropdownMenuCheckboxItem>` | -- | Additional props inherited from `DropdownMenuCheckboxItem`. |

### ContextMenuRadioGroup

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `children` | `React.ReactNode` | -- | `ContextMenuRadioItem` elements |
| `...props` | `React.ComponentPropsWithoutRef<typeof DropdownMenuRadioGroup>` | -- | Additional props inherited from `DropdownMenuRadioGroup`. |

### ContextMenuRadioItem

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `children` | `React.ReactNode` | -- | Item content |
| `...props` | `React.ComponentPropsWithoutRef<typeof DropdownMenuRadioItem>` | -- | Additional props inherited from `DropdownMenuRadioItem`. |

### ContextMenuLabel

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `className` | `string` | -- | Additional CSS classes |
| `inset` | `boolean` | -- | When `true`, adds left padding for alignment with items |
| `children` | `React.ReactNode` | -- | Label content |
| `...props` | `React.ComponentPropsWithoutRef<typeof DropdownMenuLabel>` | -- | Additional props inherited from `DropdownMenuLabel`. |

### ContextMenuSeparator

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `className` | `string` | -- | Additional CSS classes |
| `...props` | `React.ComponentPropsWithoutRef<typeof DropdownMenuSeparator>` | -- | Additional props inherited from `DropdownMenuSeparator`. |

### ContextMenuShortcut

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `keys` | `string` | -- | Keyboard shortcut string (e.g. `"ctrl+K"`) |
| `onKeysPressed` | `() => void` | -- | Callback when shortcut keys are pressed |
| `colored` | `boolean` | `false` | Whether to apply a colored background |
| `...props` | `React.ComponentPropsWithoutRef<typeof DropdownMenuShortcut>` | -- | Additional props inherited from `DropdownMenuShortcut`. |

### ContextMenuGroup

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `className` | `string` | -- | Additional CSS classes |
| `children` | `React.ReactNode` | -- | Group content |
| `...props` | `React.ComponentPropsWithoutRef<typeof DropdownMenuGroup>` | -- | Additional props inherited from `DropdownMenuGroup`. |

### ContextMenuSub

Submenu wrapper.

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `children` | `React.ReactNode` | -- | Sub-trigger and sub-content elements |
| `...props` | `React.ComponentPropsWithoutRef<typeof DropdownMenuSub>` | -- | Additional props inherited from `DropdownMenuSub`. |

### ContextMenuSubTrigger

Submenu trigger element.

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `className` | `string` | -- | Additional CSS classes |
| `inset` | `boolean` | `false` | When `true`, adds left padding for alignment |
| `children` | `React.ReactNode` | -- | Trigger content |
| `...props` | `React.ComponentPropsWithoutRef<typeof DropdownMenuSubTrigger>` | -- | Additional props inherited from `DropdownMenuSubTrigger`. |

### ContextMenuSubContent

Submenu content container.

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `className` | `string` | -- | Additional CSS classes |
| `children` | `React.ReactNode` | -- | Submenu items |
| `...props` | `React.ComponentPropsWithoutRef<typeof DropdownMenuSubContent>` | -- | Additional props inherited from `DropdownMenuSubContent`. |
