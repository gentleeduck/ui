---
title: tooltip
description: A customizable and accessible tooltip component with Floating UI positioning, state-aware styling hooks, and flexible triggers.
component: true
---

<ComponentPreview
  name="tooltip-1"
  description="tooltip"
/>

## Features

- **Floating UI powered positioning** – Smart placement with `flip`, `shift`, and `offset` middleware.
- **State-aware styling hooks** – `data-open` and `data-side` attributes for open state and placement styling.
- **Transform-origin variable** – Uses `--duck-tooltip-content-transform-origin` for direction-aware animations.
- **Customizable delays** – Control open/close delays with `delayDuration` and `skipDelayDuration`.
- **Flexible triggers** – Wrap any element using `asChild`.
- **Accessible by default** – Implements proper ARIA attributes and keyboard navigation.
- **Portal rendering** – Renders tooltips in a portal to avoid layout clipping.

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx @gentleduck/cli add tooltip
````

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install dependencies:</Step>

```bash
npm install @gentleduck/primitives @gentleduck/motion @gentleduck/variants @gentleduck/libs
```

<Step>Copy the `Tooltip` component code into your project.</Step>

<ComponentSource name="tooltip" />

<Step>Ensure import paths match your project structure.</Step>

</Steps>

</TabsContent>

</Tabs>

## Usage

```tsx
import { Tooltip, TooltipContent, TooltipTrigger } from '@/components/ui/tooltip'

<Tooltip delayDuration={500} skipDelayDuration={100}>
  <TooltipTrigger>Hover</TooltipTrigger>
  <TooltipContent>Tooltip text</TooltipContent>
</Tooltip>
```

## Examples

### Custom Trigger with `asChild`

```tsx
<Tooltip>
  <TooltipTrigger asChild>
    <span className="cursor-pointer underline">Hover me</span>
  </TooltipTrigger>
  <TooltipContent>Custom element trigger</TooltipContent>
</Tooltip>
```

### Animated Tooltip

```tsx
<Tooltip>
  <TooltipTrigger>Hover</TooltipTrigger>
  <TooltipContent className="TooltipContent">Animated tooltip</TooltipContent>
</Tooltip>
```

```css
.TooltipContent {
  transform-origin: var(--duck-tooltip-content-transform-origin);
  transition: transform 150ms ease, opacity 150ms ease;
}

.TooltipContent[data-open='false'] {
  opacity: 0;
  transform: scale(0.95);
}

.TooltipContent[data-open='true'] {
  opacity: 1;
  transform: scale(1);
}
```

## Styling Hooks

- **`data-open`** – Set on trigger and content (`true`/`false`) for state-based styling.
- **`data-side`** – Set on content (`top`, `right`, `bottom`, `left`) for placement-aware styles.
- **`--duck-tooltip-content-transform-origin`** – CSS variable for animation transform origin.

## API Reference

### **Tooltip**

* **`placement`** (`Placement`, optional, default: `'top'`): Tooltip placement relative to the trigger.
* **`open`** (`boolean`, optional): Controlled open state.
* **`defaultOpen`** (`boolean`, optional): Uncontrolled initial open state.
* **`onOpenChange`** (`(open: boolean) => void`, optional): Callback when open state changes.
* **`delayDuration`** (`number`, optional): Milliseconds delay before showing or hiding the tooltip.
* **`skipDelayDuration`** (`number`, optional): Rest time in ms before opening when the pointer stops moving.
* **`sideOffset`** (`number`, optional): Offset along the main axis.
* **`alignOffset`** (`number`, optional): Offset along the cross axis.
* **`mainAxis`** (`boolean`, optional): Enables main-axis flipping while positioning.
* **`modal`** (`boolean`, optional): Enables focus management for the tooltip content.
* **`...props`** (`React.ComponentPropsWithRef<typeof TooltipPrimitive.Root>`): Additional props forwarded to the underlying Tooltip root component.

---

### **TooltipTrigger**

* **`asChild`** (`boolean`, optional): Use the child element as the trigger.
* **`props`** (`Omit<React.ComponentPropsWithRef<typeof TooltipPrimitive.Trigger>, 'size'>`): Additional props forwarded to the tooltip trigger element.

---

### **TooltipContent**

* **`forceMount`** (`boolean`, optional, default: `true`): Keep the content mounted for CSS-driven animations.
* **`lockScroll`** (`boolean`, optional, default: `false`): Locks scroll while the tooltip is open.
* **`disabled`** (`boolean`, optional, default: `true`): Disables focus management when true.
* **`...props`** (`React.ComponentPropsWithRef<typeof TooltipPrimitive.Content>`): Additional props forwarded to the tooltip content container.

---

**Behavior:**
Provides accessible tooltip functionality by wrapping the primitive tooltip components and positioning content with Floating UI. The trigger and content receive `data-open`, while the content also receives `data-side` and `--duck-tooltip-content-transform-origin` to support stateful styling and animations. Tooltips open on hover and focus by default; if you need click-to-toggle, control `open` and `onOpenChange` in your own click handler.
