---
title: select
description: Displays a list of options for the user to pick from -- triggered by a button.
component: true
---

<ComponentPreview
  name="select-1"
  description="A select component with a list of options."
/>

## Philosophy

Native selects are accessible but difficult to style consistently. This select keeps accessible interaction patterns while using a compositional approach (`Popover` + command-style keyboard handling) so trigger, content, and items remain fully customizable.

## How It's Built

<MermaidDiagram chart={`graph TD
  Select[Select]
  Select --> primitives["@gentleduck/primitives\n/popover"]:::internal
  Select --> libs["@gentleduck/libs"]:::internal
  Select --> Button["Button"]:::component
  Select --> lucide["lucide-react"]:::external

  primitives --> |"provides"| Floating["Floating UI\nPositioning"]
  primitives --> |"provides"| Dismiss["Click Outside\nDismiss"]
  Select --> |"reuses"| CommandHooks["Command's\nuseHandleKeyDown"]:::component
  Select --> |"custom"| ScrollHook["useSelectScroll"]
  Select --> |"uses"| Context["React Context"]

  classDef external fill:#818cf8,stroke:#6366f1,color:#fff
  classDef internal fill:#2dd4bf,stroke:#14b8a6,color:#fff
  classDef component fill:#fb923c,stroke:#f97316,color:#fff
`} />

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx @gentleduck/cli add select
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install @gentleduck/primitives  @gentleduck/libs
```

<Step>Add the `Command`, `Button` and `Popover` component to your project.</Step>

The `Select` component uses the [`Command`](/components/command), [`Button`](/components/button)
and [`Popover`](/components/popover) components. Make sure you have them installed in your project.

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="select" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</Tabs>

## Usage

```tsx
import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from "@/components/ui/select"
```

```tsx
<Select>
  <SelectTrigger className="w-[180px]">
    <SelectValue placeholder="Theme" />
  </SelectTrigger>
  <SelectContent>
    <SelectItem value="light">Light</SelectItem>
    <SelectItem value="dark">Dark</SelectItem>
    <SelectItem value="system">System</SelectItem>
  </SelectContent>
</Select>
```

## Examples

### Scrollable

<ComponentPreview
  name="select-2"
  description="A select component with a scrollable list of options."
/>

### Form

<ComponentPreview name="select-3" />

## Component Composition

<MermaidDiagram chart={`graph TD
    A[Select] --> B[SelectTrigger]
    A --> C[SelectContent]
    B --> D[SelectValue]
    C --> E[SelectGroup]
    C --> F[SelectSeparator]
    C --> G[SelectScrollUpButton]
    C --> H[SelectScrollDownButton]
    E --> I[SelectLabel]
    E --> J[SelectItem]
`} />

## API Reference

### Select

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `value` | `string` | -- | Current selected value of the select component |
| `defaultValue` | `string` | `''` | Uncontrolled initial value |
| `onValueChange` | `(value: string) => void` | -- | Callback fired when the selected value changes |
| `open` | `boolean` | -- | Controlled open state |
| `onOpenChange` | `(open: boolean) => void` | -- | Callback fired when dropdown open state changes |
| `scrollable` | `boolean` | `false` | Enables scroll buttons and makes the dropdown content scrollable |

### SelectWrapper

Internal component managing refs, selection state, and event handlers. Uses `SelectContext` to expose state and refs. Handles initialization of items and groups, manages highlighting and selection.

### SelectTrigger

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `customIndicator` | `React.ReactNode` | -- | Custom icon to display on the trigger instead of the default ChevronDown |
| `className` | `string` | -- | Additional CSS class names |
| `children` | `React.ReactNode` | -- | Trigger content |
| `...props` | `React.ComponentPropsWithoutRef<typeof PopoverTrigger>` | -- | Additional props inherited from `PopoverTrigger`. |

### SelectContent

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `className` | `string` | -- | Additional CSS class names |
| `children` | `React.ReactNode` | -- | Content to render inside the dropdown |
| `side` | `'top' \| 'right' \| 'bottom' \| 'left'` | `'bottom'` | Preferred side relative to the trigger |
| `align` | `'start' \| 'center' \| 'end'` | `'center'` | Alignment on the chosen side |
| `sideOffset` | `number` | `4` | Main-axis offset from trigger |
| `alignOffset` | `number` | `0` | Cross-axis offset from trigger |
| `...props` | `React.ComponentPropsWithoutRef<typeof PopoverContent>` | -- | Additional props inherited from `PopoverContent`. |

### SelectGroup

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `className` | `string` | -- | Additional CSS class names |
| `children` | `React.ReactNode` | -- | Group content (typically `SelectLabel` and `SelectItem` elements) |
| `...props` | `React.HTMLProps<HTMLUListElement>` | -- | Additional props to spread to the ul element |

### SelectValue

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `placeholder` | `string \| number` | -- | Text to show when no value is selected |
| `className` | `string` | -- | Additional CSS class names |
| `children` | `React.ReactNode` | -- | Custom content to render |
| `...props` | `Omit<React.HTMLProps<HTMLDivElement>, 'placeholder'>` | -- | Additional props to spread to the content div |

### SelectLabel

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `htmlFor` | `string` | -- | ID of the associated input or control |
| `className` | `string` | -- | Additional CSS class names |
| `children` | `React.ReactNode` | -- | Label text content |
| `...props` | `React.HTMLProps<HTMLLabelElement>` | -- | Additional props to spread to the label element |

### SelectItem

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `value` | `string` | (required) | The value representing this item |
| `disabled` | `boolean` | -- | Whether the item is disabled |
| `className` | `string` | -- | Additional CSS class names |
| `children` | `React.ReactNode` | -- | Item content |
| `...props` | `Omit<React.HTMLProps<HTMLLIElement>, 'value'>` | -- | Additional props to spread to the li element |

### SelectSeparator

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `className` | `string` | -- | Additional CSS class names |
| `...props` | `React.HTMLProps<HTMLDivElement>` | -- | Additional props to spread to the content div |

### SelectScrollUpButton / SelectScrollDownButton

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `className` | `string` | -- | Additional CSS class names |
| `...props` | `React.ComponentPropsWithoutRef<typeof Button>` | -- | Additional props inherited from `Button`. |

### SelectContext

The context exposes:

| Property | Type | Description |
| --- | --- | --- |
| `open` | `boolean` | Dropdown open state |
| `value` | `string` | Current selected value |
| `triggerRef` | `React.RefObject<HTMLButtonElement \| null>` | Ref to the trigger button |
| `contentRef` | `React.RefObject<HTMLDivElement \| null>` | Ref to the scrollable dropdown content `<div>` |
| `groupsRef` | `React.RefObject<HTMLUListElement[] \| null>` | Ref to groups `<ul>` elements |
| `itemsRef` | `React.RefObject<HTMLLIElement[] \| null>` | Ref to the list of select items `<li>` |
| `selectedItem` | `HTMLLIElement \| null` | Currently selected item DOM element |
| `scrollable` | `boolean` | If the dropdown is scrollable |
