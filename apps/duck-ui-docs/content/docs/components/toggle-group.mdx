---
title: toggle-group
description: A set of two-state buttons that can be toggled on or off.
component: true
---

<ComponentPreview
  name="toggle-group-1"
  description="A toggle group with three items."
/>

## Philosophy

When multiple toggles share context, they become a group. ToggleGroup manages mutual exclusivity (single mode) or multi-selection, keeping the state logic out of your components. It's the same relationship as RadioGroup to Radio -- the group owns the state, the items render the UI.

## How It's Built

<MermaidDiagram chart={`graph TD
  ToggleGroup[ToggleGroup]
  ToggleGroup --> variants["@gentleduck/variants"]:::internal
  ToggleGroup --> libs["@gentleduck/libs"]:::internal
  ToggleGroup --> Toggle["Toggle"]:::component
  variants --> |"provides"| VariantSystem["Type-safe Variants"]
  Toggle --> |"provides"| ItemBehavior["Individual\nToggle Behavior"]
  ToggleGroup --> |"manages"| GroupContext["Selection Context\n(single/multiple)"]
  classDef internal fill:#2dd4bf,stroke:#14b8a6,color:#fff
  classDef component fill:#fb923c,stroke:#f97316,color:#fff
`} />

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx @gentleduck/cli add toggle-group
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies:</Step>

```bash
npm install @gentleduck/variants @gentleduck/libs
```

<Step>Add the [`Toggle`](/docs/components/toggle) component to your project </Step>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="toggle-group" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</Tabs>

## Usage

```tsx
import { ToggleGroup, ToggleGroupItem } from '@/components/ui/toggle-group'
```

```tsx
<ToggleGroup type="single">
  <ToggleGroupItem value="a">A</ToggleGroupItem>
  <ToggleGroupItem value="b">B</ToggleGroupItem>
  <ToggleGroupItem value="c">C</ToggleGroupItem>
</ToggleGroup>
```

## Examples

### Default

<ComponentPreview
  name="toggle-group-2"
  description="A toggle group with three items."
/>

### Outline

<ComponentPreview
  name="toggle-group-3"
  description="A toggle group using the outline variant."
/>

### Single

<ComponentPreview
  name="toggle-group-4"
  description="A toggle group with single selection."
/>

### Small

<ComponentPreview
  name="toggle-group-5"
  description="A toggle group using the small size."
/>

### Large

<ComponentPreview
  name="toggle-group-6"
  description="A toggle group using the large size."
/>

### Disabled

<ComponentPreview
  name="toggle-group-7"
  description="A disabled toggle group."
/>

## API Reference

### ToggleGroup

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `className` | `string` | -- | Additional CSS class names to apply |
| `variant` | `'default' \| 'outline'` | `'default'` | Visual style variant for the toggles (passed down to items via context) |
| `size` | `'default' \| 'sm' \| 'lg'` | -- | Size variant for the toggles (passed down to items via context) |
| `type` | `'single' \| 'multiple'` | -- | Determines selection behavior. `'single'` allows only one toggle selected at a time; `'multiple'` allows multiple toggles selected |
| `onValueChange` | `(value: string) => void` | -- | Callback invoked when a toggle item is selected or deselected |
| `value` | `string \| string[]` | -- | Controlled selected value(s) |
| `defaultValue` | `string \| string[]` | -- | Uncontrolled initial selected value(s) |
| `children` | `React.ReactNode` | -- | ToggleGroupItem elements to render inside the group |
| `ref` | `React.Ref<HTMLUListElement>` | -- | Ref forwarded to the underlying `ul` element |
| `...props` | `Omit<React.HTMLProps<HTMLUListElement>, 'size'>` | -- | Additional props to spread to the ul element |

### ToggleGroupItem

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `className` | `string` | -- | Additional CSS class names to apply |
| `variant` | `'default' \| 'outline'` | -- | Visual style variant for this item (overrides group context) |
| `size` | `'default' \| 'sm' \| 'lg'` | -- | Size variant for this item (overrides group context) |
| `value` | `string` | (required) | The unique value identifying this toggle item |
| `children` | `React.ReactNode` | -- | Content rendered inside the toggle item |
| `ref` | `React.Ref<HTMLInputElement>` | -- | Ref forwarded to the underlying Toggle component |
| `...props` | `React.ComponentPropsWithRef<typeof Toggle>` | -- | Additional props inherited from `Toggle`. |

## RTL Support

<ComponentPreview
  name="toggle-group-8"
  description="A toggle group with RTL support."
/>

