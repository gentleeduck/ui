---
title: "Lesson 3: Building a Grid"
description: "Using buildCalendarMonth to generate and render a calendar month grid."
---

<Callout icon={<Lightbulb />}>

**Lesson 3 of 8**: You will use `buildCalendarMonth` to generate a month grid and render it as a table.

</Callout>

## The grid builder

`buildCalendarMonth` is a pure function that takes an adapter, a date, and optional config, and returns a `CalendarMonth` object with weeks and day cells.

```tsx showLineNumbers
import { NativeAdapter, buildCalendarMonth } from '@gentleduck/calendar'

const adapter = new NativeAdapter()
const march2026 = new Date(2026, 2, 1)

const month = buildCalendarMonth(adapter, march2026, {
  showOutsideDays: true,
  fixedWeeks: false,
})

// month.weeks is an array of CalendarWeek objects
// Each week has 7 CalendarDay objects
```

---

## What you get back

```tsx showLineNumbers
// CalendarMonth<Date>
{
  month: Date,          // First day of the month
  weeks: [
    {
      weekNumber: 9,    // ISO week number
      days: [
        {
          date: Date,           // The date
          isToday: false,       // Is this today?
          isOutsideMonth: true, // From adjacent month?
          isWeekend: true,      // Saturday or Sunday?
        },
        // ... 6 more days
      ]
    },
    // ... more weeks
  ]
}
```

---

## Rendering the grid

Here is a minimal React component that renders the grid:

```tsx showLineNumbers
import { NativeAdapter, buildCalendarMonth } from '@gentleduck/calendar'

const adapter = new NativeAdapter()

function StaticCalendar({ date }: { date: Date }) {
  const month = buildCalendarMonth(adapter, date, {
    showOutsideDays: true,
  })

  return (
    <table>
      <thead>
        <tr>
          {['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat'].map(d => (
            <th key={d}>{d}</th>
          ))}
        </tr>
      </thead>
      <tbody>
        {month.weeks.map((week, i) => (
          <tr key={i}>
            {week.days.map(day => (
              <td
                key={day.date.getTime()}
                style={{
                  opacity: day.isOutsideMonth ? 0.4 : 1,
                  fontWeight: day.isToday ? 'bold' : 'normal',
                }}>
                {day.date.getDate()}
              </td>
            ))}
          </tr>
        ))}
      </tbody>
    </table>
  )
}
```

---

## Multi-month grids

Use `buildMultiMonth` to generate multiple consecutive months:

```tsx showLineNumbers
import { buildMultiMonth } from '@gentleduck/calendar'

const months = buildMultiMonth(adapter, new Date(), 3, {
  showOutsideDays: true,
})

// months[0] -- current month
// months[1] -- next month
// months[2] -- month after next
```

---

## Grid config options

| Option | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `showOutsideDays` | `boolean` | `true` | Include days from adjacent months |
| `fixedWeeks` | `boolean` | `false` | Always produce 6 weeks |
| `weekStartDay` | `WeekStartDay` | `0` | 0 = Sunday, 1 = Monday, etc. |

<Callout icon={<Lightbulb />}>

`buildCalendarMonth` is a pure function. It does not manage state. In the next lesson, we will use `useCalendar` to add interactivity.

</Callout>

<Callout icon={<MapIcon />}>

Next: [Lesson 4 -- Selection Modes](/docs/packages/duck-calendar/course/04-selection-modes)

</Callout>
