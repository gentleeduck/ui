---
title: gentleduck/cli
description: Use the gentleduck CLI package to add components and dependencies to your project.
---

<MermaidDiagram chart={`flowchart TD
    A[npx @gentleduck/cli] --> B[init]
    A --> C[add]
    B --> D[Configure Project]
    C --> E[Select Components]
    E --> F[Install Dependencies]
    F --> G[Copy Source Files]
`} />

## Philosophy

A good CLI makes adoption frictionless. gentleduck/cli doesn't install pre-built packages -- it copies source code into your project. This means you own every line, can modify anything, and never wait for a library release to fix a bug. The tradeoff is intentional: full control over zero-config convenience.

---

## init

Use the `init` command to initialize configuration and dependencies for a new project.

```bash
npx @gentleduck/cli init [components...]
````

**Options**

```bash
Usage: @gentleduck/cli init [options] [components...]

init the project

Arguments:
  [components...]   names, url or local path to component to install. When not provided, you’ll be directed to a list to select from.

Options:
  -y, --yes           skip confirmation prompt. (default: false)
  -c, --cwd <cwd>     the working directory. defaults to the current directory. (default: process.cwd())
  -f, --force         overwrite existing components. (default: false)
```

---

## add

Use the `add` command to add components and dependencies to your project.

```bash
npx @gentleduck/cli add [components...]
```

**Options**

```bash
Usage: @gentleduck/cli add [options] [components...]

add components to your project

Arguments:
  [components...]   names, url or local path to component to install. When not provided, you’ll be directed to a list to select from.

Options:
  -y, --yes           skip confirmation prompt. (default: false)
  -f, --force         overwrite existing components. (default: false)
```

```
