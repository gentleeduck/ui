}>

Format key binding strings for display with platform-aware modifier labels. Converts internal binding strings like `mod+shift+s` into user-facing text like 
```

}>

For a ready-made keyboard shortcut display component, see the `<Kbd>` component available in the gentleduck/ui component library.